;; # Hello, Clerk 👋

;; Clerk gives you a rich notebook experience, built on top of *regular* Clojure namespaces, enhanced with markdown comments.
(def a 39)

(+ a 3)

;; Here's an expression that takes about three seconds to evaluate.
(do (Thread/sleep 3000)
    (shuffle (range 100)))

;; We can write more, and still get instant feedback!
