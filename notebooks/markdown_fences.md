# 🤺 Markdown Fences

```
'(evaluated :and "highlighted")
```

```clojure
'(evaluated :and "highlighted")
```

```clojure eval=false
'(1 2 "not evaluated" :but-still-highlighted)
```

```clojure skip
'(1 2 "not evaluated" :but-still-highlighted)
```

```js
() => {
  if (true) {
    return 'not evaluated'
  } else {
    return 'what'
  }
}
```
